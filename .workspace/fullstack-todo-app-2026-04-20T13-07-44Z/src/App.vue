### Step 12: Verify the Application

1. Ensure all semantic elements are present (header, main, footer, etc.).
2. Ensure all `data-testid` attributes exist and match the contract.
3. Ensure all user flows work (type into inputs, click buttons, see results update).
4. Ensure all assertions from user flows pass (counts update, classes change, etc.).

### Step 13: Run Unit Tests

Let's run the unit tests to make sure everything is working as expected.